"use client";

// New project upload form — all fields + media upload
export function UploadForm() {
  // TODO: React Hook Form + uploadProjectSchema validation
  // useUpload() for media progress, createProject() on submit
  return (
    <form className="glass p-6 space-y-4">
      <p className="text-text-muted text-body-sm">Upload form coming in Week 3–4 build phase.</p>
    </form>
  );
}
