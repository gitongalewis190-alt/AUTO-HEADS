"use client";

export function LoginForm() {
  // TODO: loginSchema, loginAction, Google sign-in button
  return (
    <div className="glass p-8 w-full max-w-md space-y-6">
      <h1 className="text-h2 font-heading font-bold text-center">Login</h1>
      <p className="text-text-muted text-body-sm text-center">Login form coming in Week 3 build phase.</p>
    </div>
  );
}
