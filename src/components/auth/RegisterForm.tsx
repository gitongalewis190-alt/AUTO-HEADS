"use client";

export function RegisterForm() {
  // TODO: registerSchema, registerAction, logo upload via useUpload()
  return (
    <div className="glass p-8 w-full max-w-md space-y-6">
      <h1 className="text-h2 font-heading font-bold text-center">Create Account</h1>
      <p className="text-text-muted text-body-sm text-center">Registration form coming in Week 3 build phase.</p>
    </div>
  );
}
